// ─────────────────────────────────────────────────────────────
//  Siggy – API Configuration
//  Edit SIGGY_API_URL to point to wherever your local server
//  is exposed (ngrok, cloudflared, localhost, etc.)
// ─────────────────────────────────────────────────────────────

const SIGGY_CONFIG = {
  // When running locally only:
  //   API_URL: "http://localhost:5000/api"
  //
  // When using ngrok (replace with your current tunnel URL):
  //   API_URL: "https://xxxx-xx-xx-xxx-xx.ngrok-free.app/api"
  //
  // When using cloudflared (replace with your tunnel hostname):
  //   API_URL: "https://your-tunnel.trycloudflare.com/api"

  API_URL: "https://martina-unperuked-khloe.ngrok-free.dev/api"
};
